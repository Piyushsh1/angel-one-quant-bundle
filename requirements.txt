smartapi-python==1.5.5
logzero==1.7.0
websocket-client==1.8.0
pyotp==2.9.0
python-dotenv==1.0.1
pandas-ta==0.4.71b0
pandas>=2.2,<3.1
numpy>=1.26,<2.3
requests==2.32.3

# ── PostgreSQL (single application database, one per environment) ──────────
# psycopg 3 with the binary wheel so no local libpq build is required.
psycopg[binary,pool]==3.2.3
